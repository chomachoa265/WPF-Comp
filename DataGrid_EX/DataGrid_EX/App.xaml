<Application x:Class="DataGrid_EX.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:DataGrid_EX"
             xmlns:localM="clr-namespace:DataGrid_EX.Models"
             StartupUri="/Views/MainWindow.xaml"
             >
    <Application.Resources>
        <!--<DataTemplate x:Key="DynamicHeaderTemplate" DataType="{x:Type localM:CustomColumnHeader}">
            <Border BorderBrush="Gray" BorderThickness="0,0,0,2" HorizontalAlignment="Stretch">
                <UniformGrid Rows="3" Background="AliceBlue">
                    <TextBlock Text="{Binding Title}" FontWeight="Bold" Foreground="#333" HorizontalAlignment="Stretch"/>
                    <TextBlock Text="{Binding SubTitle}" FontWeight="Bold" Foreground="#333"/>
                    <TextBlock Text="{Binding Unit}" FontWeight="Bold" Foreground="#333"/>
                </UniformGrid>
            </Border>
        </DataTemplate>-->
        <DataTemplate x:Key="DynamicHeaderTemplate">
            <Grid Background="Transparent">
                <TextBlock Text="{Binding Header}" 
                       FontWeight="Bold" 
                       VerticalAlignment="Center">
                    <TextBlock.InputBindings>
                        <MouseBinding Gesture="LeftClick" 
                                  Command="{Binding Command}" 
                                  CommandParameter="{Binding}" />
                    </TextBlock.InputBindings>
                </TextBlock>
            </Grid>
        </DataTemplate>
    </Application.Resources>
</Application>
